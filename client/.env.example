# Local development only
VITE_API_URL=http://localhost:5000/api
VITE_AUTH_URL=http://localhost:5000/api/auth

# Vercel production — leave VITE_* unset, or use:
# VITE_API_URL=/api
# VITE_AUTH_URL=/api/auth
# (The app converts these to full URLs for Better Auth automatically.)

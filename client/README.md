# Deresegn — Receipt Checker

Frontend client for Deresegn receipt verification service.

## Setup

```bash
cd client
npm install
```

Create a `.env` file with required variables (see `.env` for template).

## Development

```bash
npm run dev
```

Runs on http://localhost:5173

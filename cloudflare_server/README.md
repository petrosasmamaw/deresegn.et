# Deresegn — Receipt Checker

Backend server for Deresegn receipt verification service.

## Setup

```bash
cd server
npm install
```

Create a `.env` file with required variables (see `.env` for template).

## Development

```bash
npm run dev
```

## Database

```bash
npm run db:migrate
npm run db:studio
```
